<Page x:Class="Connect4.Views.MainMenuView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Connect4.Views"
      xmlns:conv="clr-namespace:Connect4.Converters"
      mc:Ignorable="d" 
      d:DesignHeight="768" d:DesignWidth="1024"
      Title="Connect4">

    <Page.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="25"
                            Padding="{TemplateBinding Padding}"
                            RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="border"
                                                         Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                                         To="1.1" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="border"
                                                         Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                                         To="1.1" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="border"
                                                         Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                                         To="1" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="border"
                                                         Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                                         To="1" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Gray"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Height" Value="50"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#FFDDEEFF" Offset="0"/>
                <GradientStop Color="#FFAACCFF" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <TextBlock FontSize="100" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,100,0,40" TextAlignment="Center">
            <TextBlock.Text>
                Connect 4
            </TextBlock.Text>
            <TextBlock.Foreground>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="Red" Offset="0"/>
                    <GradientStop Color="Yellow" Offset="0.5"/>
                    <GradientStop Color="Orange" Offset="1"/>
                </LinearGradientBrush>
            </TextBlock.Foreground>
            <TextBlock.Effect>
                <DropShadowEffect Color="Black" ShadowDepth="4" BlurRadius="8" Opacity="0.7"/>
            </TextBlock.Effect>
        </TextBlock>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="200">
            <Separator Opacity="0" Height="20"/>
            <Button Content="Play" Style="{StaticResource RoundedButton}" Margin="0,20,0,0" Command="{Binding PlayCommand}">
                <Button.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFF5252" Offset="0"/>
                        <GradientStop Color="#FFD32F2F" Offset="1"/>
                    </LinearGradientBrush>
                </Button.Background>
            </Button>
            <Separator Opacity="0" Height="20"/>
            <Button Content="My Games" Style="{StaticResource RoundedButton}" Margin="0,10,0,0" Command="{Binding MyGamesCommand}">
                <Button.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFFC107" Offset="0"/>
                        <GradientStop Color="#FFFF8F00" Offset="1"/>
                    </LinearGradientBrush>
                </Button.Background>
            </Button>
            <Separator Opacity="0" Height="20"/>
            <Button Content="Quit" Style="{StaticResource RoundedButton}" Margin="0,10,0,0" Command="{Binding QuitCommand}">
                <Button.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFB0BEC5" Offset="0"/>
                        <GradientStop Color="#FF78909C" Offset="1"/>
                    </LinearGradientBrush>
                </Button.Background>
            </Button>
        </StackPanel>

        <Button Width="60" Height="60"
        HorizontalAlignment="Right"
        VerticalAlignment="Bottom"
        Margin="60"
        Command="{Binding ToggleSoundCommand}">
            <Button.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FF42A5F5" Offset="0"/>
                    <GradientStop Color="#FF1E88E5" Offset="1"/>
                </LinearGradientBrush>
            </Button.Background>

            <Grid>
                <!-- Speaker Icon -->
                <Viewbox Width="30" Height="30">
                    <Canvas Width="24" Height="24">
                        <Path Fill="White" Data="M3,8 L7,8 L12,4 L12,20 L7,16 L3,16 Z"/>
                        <Path Fill="White" Data="M14,8 C15.5,10 15.5,14 14,16"/>
                        <Path Fill="White" Data="M16,6 C18,10 18,14 16,18"/>
                    </Canvas>
                </Viewbox>

                <!-- Red line crossing out the speaker -->
                <Line X1="0" Y1="30" X2="30" Y2="0"
                  Stroke="Red" StrokeThickness="4"
                  Visibility="{Binding IsSoundOff, Converter={StaticResource BoolToVis}}"/>
            </Grid>
        </Button>
    </Grid>
</Page>

<Page x:Class="Connect4.Views.GameSettingsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:conv="clr-namespace:Connect4.Converters"
      xmlns:local="clr-namespace:Connect4.Views"
      mc:Ignorable="d" 
      d:DesignHeight="768" d:DesignWidth="1024"
      Title="Connect4">

    <Page.Resources>
        <conv:EnumToBoolConverter x:Key="EnumToBoolConverter"/>

        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="25"
                            Padding="{TemplateBinding Padding}"
                            RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="border"
                                                         Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                                         To="1.1" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="border"
                                                         Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                                         To="1.1" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="border"
                                                         Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                                         To="1" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="border"
                                                         Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                                         To="1" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Gray"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Height" Value="50"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#FFDDEEFF" Offset="0"/>
                <GradientStop Color="#FFAACCFF" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>


        <TextBlock FontSize="100" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,20,0,40" TextAlignment="Center">
            <TextBlock.Text>
                Game Settings
            </TextBlock.Text>
            <TextBlock.Foreground>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="Red" Offset="0"/>
                    <GradientStop Color="Yellow" Offset="0.5"/>
                    <GradientStop Color="Orange" Offset="1"/>
                </LinearGradientBrush>
            </TextBlock.Foreground>
            <TextBlock.Effect>
                <DropShadowEffect Color="Black" ShadowDepth="4" BlurRadius="8" Opacity="0.7"/>
            </TextBlock.Effect>
        </TextBlock>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="250">
            <Separator Opacity="0" Height="120"/>
            <Border Background="#FFFFF0F0" CornerRadius="15" Padding="10">
                <StackPanel>
                    <TextBlock Text="Player Names" FontSize="20" FontWeight="Bold" Foreground="#FFB71C1C" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBox Text="{Binding GameSettings.RedPlayerName, UpdateSourceTrigger=PropertyChanged}" MaxLength="12" Margin="0,5" Padding="5" FontSize="16" Background="#FFEF5350" BorderThickness="0" />
                    <TextBox Text="{Binding GameSettings.YellowPlayerName, UpdateSourceTrigger=PropertyChanged}" MaxLength="12" Margin="0,5" Padding="5" FontSize="16" Background="#FFFFB300" BorderThickness="0" />
                </StackPanel>
            </Border>

            <Separator Opacity="0" Height="10"/>

            <Border Background="#FFFFFDE7" CornerRadius="15" Padding="10">
                <StackPanel>
                    <TextBlock Text="Starting Player" FontSize="20" FontWeight="Bold" 
                   Foreground="#FFF57F17" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <RadioButton GroupName="StartingPlayer" IsChecked="{Binding GameSettings.StartingPlayer, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Red}">
                            <RadioButton.Template>
                                <ControlTemplate TargetType="RadioButton">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Ellipse x:Name="chip" Width="25" Height="25" Fill="Red" Stroke="Red" StrokeThickness="3" RenderTransformOrigin="0.5,0.5">
                                            <Ellipse.RenderTransform>
                                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                                            </Ellipse.RenderTransform>
                                        </Ellipse>
                                        <TextBlock Text="Red" FontSize="16" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                    </StackPanel>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="chip" Property="Stroke" Value="Black"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="chip" Property="RenderTransform">
                                                <Setter.Value>
                                                    <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </RadioButton.Template>
                        </RadioButton>

                        <Separator Opacity="0" Width="50"/>

                        <RadioButton GroupName="StartingPlayer" IsChecked="{Binding GameSettings.StartingPlayer, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Yellow}">
                            <RadioButton.Template>
                                <ControlTemplate TargetType="RadioButton">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Ellipse x:Name="chip" Width="25" Height="25" Fill="Goldenrod" Stroke="Goldenrod" StrokeThickness="3" RenderTransformOrigin="0.5,0.5">
                                            <Ellipse.RenderTransform>
                                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                                            </Ellipse.RenderTransform>
                                        </Ellipse>
                                        <TextBlock Text="Yellow" FontSize="16" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                    </StackPanel>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="chip" Property="Stroke" Value="Black"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="chip" Property="RenderTransform">
                                                <Setter.Value>
                                                    <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </RadioButton.Template>
                        </RadioButton>

                    </StackPanel>
                </StackPanel>
            </Border>

            <Separator Opacity="0" Height="10"/>

            <Border Background="#FFE3F2FD" CornerRadius="15" Padding="10">
                <StackPanel>
                    <TextBlock Text="Time Control" FontSize="20" FontWeight="Bold" Foreground="#FF1976D2" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5">
                        <TextBlock Text="Base Time (min):" VerticalAlignment="Center" FontSize="16" Margin="0,0,10,0"/>
                        <ComboBox Width="100" FontSize="16" Background="#FFBBDEFB" BorderThickness="0" Padding="3" SelectedValue="{Binding GameSettings.BaseTimeMinutes, Mode=TwoWay}" SelectedValuePath="Content">
                            <ComboBoxItem Content="1"/>
                            <ComboBoxItem Content="3"/>
                            <ComboBoxItem Content="5"/>
                            <ComboBoxItem Content="10"/>
                            <ComboBoxItem Content="15"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5">
                        <TextBlock Text="Increment (sec): " VerticalAlignment="Center" FontSize="16" Margin="0,0,10,0"/>
                        <ComboBox Width="100" FontSize="16" Background="#FFFFF9C4" BorderThickness="0" Padding="3" SelectedValue="{Binding GameSettings.IncrementSeconds, Mode=TwoWay}" SelectedValuePath="Content">
                            <ComboBoxItem Content="0"/>
                            <ComboBoxItem Content="1"/>
                            <ComboBoxItem Content="3"/>
                            <ComboBoxItem Content="5"/>
                        </ComboBox>
                    </StackPanel>

                </StackPanel>
            </Border>


            <Separator Opacity="0" Height="10"/>
            <Button Content="Play" Style="{StaticResource RoundedButton}" Margin="0,20,0,0" Command="{Binding PlayCommand}">
                <Button.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFF5252" Offset="0"/>
                        <GradientStop Color="#FFD32F2F" Offset="1"/>
                    </LinearGradientBrush>
                </Button.Background>
            </Button>
            <Separator Opacity="0" Height="10"/>
            <Button Content="Back" Style="{StaticResource RoundedButton}" Margin="0,10,0,0" Command="{Binding BackCommand}">
                <Button.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFB0BEC5" Offset="0"/>
                        <GradientStop Color="#FF78909C" Offset="1"/>
                    </LinearGradientBrush>
                </Button.Background>
            </Button>
        </StackPanel>
    </Grid>
</Page>
